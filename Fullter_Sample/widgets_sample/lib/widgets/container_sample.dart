import 'package:flutter/material.dart';
import 'package:widgets_sample/widgets/base_sample.dart';

class ContainerSample extends BaseSample {
  ContainerSample(String title) : super(title);
  @override
  State<StatefulWidget> createState() => new _ContainerSampleState(title);
}

class _ContainerSampleState extends BaseSampleState {
  _ContainerSampleState(String title) : super(title);

  @override
  Widget getBody() {
    // TODO: implement getBody
    return super.getBody();
  }
}
